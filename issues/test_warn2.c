#include "test.h"

static void test_func(const char *loc_str) {
    if (0) {
        char lang[64];
        const char *remainder = loc_str;
        int dummy = lang[0] + remainder[0];
    }
}

int main() {
    return 0;
}
